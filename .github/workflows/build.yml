name: Build Windows Executable

on:
  push:
    branches: [main, master]
    tags:
      - 'v*'
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pyinstaller

      - name: Build with Flet pack
        run: |
          pip install flet
          flet pack subtitlelab/main.py --name SubtitleLab --icon assets/icon.ico 2>$null || flet pack subtitlelab/main.py --name SubtitleLab
        continue-on-error: true

      - name: Fallback to PyInstaller if Flet pack fails
        if: failure() || !hashFiles('dist/SubtitleLab.exe')
        run: |
          pyinstaller --name SubtitleLab --onefile --windowed --clean --noconfirm `
            --hidden-import=asyncio `
            --hidden-import=aiofiles `
            --hidden-import=openai `
            --hidden-import=pysubs2 `
            --hidden-import=tenacity `
            --hidden-import=pydantic `
            --hidden-import=tiktoken `
            --hidden-import=tiktoken_ext `
            --hidden-import=tiktoken_ext.openai_public `
            --hidden-import=regex `
            --hidden-import=certifi `
            --hidden-import=httpx `
            --hidden-import=httpcore `
            --hidden-import=anyio `
            --hidden-import=sniffio `
            --hidden-import=h11 `
            --hidden-import=flet `
            --hidden-import=flet.core `
            --hidden-import=flet.utils `
            --collect-all=flet `
            --collect-data=tiktoken `
            --collect-data=certifi `
            subtitlelab/main.py

      - name: Verify executable exists
        run: |
          if (Test-Path "dist/SubtitleLab.exe") {
            Write-Host "Build successful: dist/SubtitleLab.exe"
            Get-Item "dist/SubtitleLab.exe" | Select-Object Name, Length
          } else {
            Write-Host "Build failed: executable not found"
            exit 1
          }

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: SubtitleLab-Windows
          path: dist/SubtitleLab.exe
          retention-days: 30

  release:
    needs: build-windows
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    
    permissions:
      contents: write
    
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: SubtitleLab-Windows

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: SubtitleLab.exe
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
